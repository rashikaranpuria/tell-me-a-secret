import React from 'react';
import { Text, View } from 'react-native';

const InfoActivity = () => (
    <View>
      <Text>Because some things should exist.</Text>
    </View>
  );

export default InfoActivity;
